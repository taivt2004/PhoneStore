import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    btn_add_to_cart: {
        width: '65%',
        padding: 10,
        backgroundColor: '#F98956',
        justifyContent: 'center',
        alignItems: 'center',
        borderWidth: 1,
        borderColor: 'orange',
        borderWidth: 1,
    }
})

export default styles